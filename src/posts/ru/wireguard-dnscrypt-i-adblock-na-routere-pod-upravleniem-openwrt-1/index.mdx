import { LightBox } from 'src/components/organisms/LightBox';

export const meta = {
  title:
    'WireGuard, DNSCrypt и AdBlock на роутере под управлением OpenWRT － часть 1',
  description:
    'Как фронтендеру позаботиться о собственной сетевой безопасности. Часть 1.',
  keywords: [
    'операционки',
    'wireguard',
    'vpn',
    'openwrt',
    'dnscrypt-proxy',
    'adblock',
  ],
  genre: ['Безопасность', 'Операционные системы'],
  icon: '👩‍💻',
  cover: require('./assets/openwrt-luci.png'),
  datePublished: '2020-04-09 00:00:00',
  dateModified: '2020-04-09 00:00:00',
};

### Table Of Contents

### Предыстория

Последние события весны 2020 и ситуация с короновирусом вынудили меня, как и многих других, перейти на удаленную работу из дома. Появилось немного больше сводобного времени, часть которого я решил потратить на кое что, что уже давно планировал сделать - настроить домашний маршрутизатор.

До какого-то момента я даже не задумывался, что роутер может быть чем-то большим, чем просто черным яшиком висящим на стене, и дающим доступ в интернет. Этот "ящик" вполне может взять на себя некоторые обязанности по организации домашней сети.

Вот небольшой список задач, которые я решил переложить на роутер:

1. Автоматическое перенаправление определенного трафика в VPN туннель.
2. Шифрование DNS запросов.
3. Блокировка рекламы.
4. Организация домашней медиатеки.

Для всего этого нам понадобиться немного больше функциональности, чем могут предоставить большинство тех роутеров, которые вам доводилось видеть в домах и офисах.
Поэтому первый шаг - прошить имеющийся роутер на что-то более подходящее.

### Выбираем операционную систему

[OpenWRT](https://openwrt.org/) - это операционная система Linux для встраеваемых устройств.
Она максимально компактная и может запускаться на устройствах с минимум оперативной и постоянной памяти.
Но все же у нее есть минимальные требования - 8MB флеш-памяти и 32MB RAM.
Поэтому сперва нужно удостовериться, что имеющийся дома маршрутизатор удовлетворяет требованиям. Это не всегда так.

Рекомендую поискать в сети ветки форумов с обсуждением своей модели, и заодно посмотреть, какие проблемы могут возникнуть при смене прошивки.

К слову, OpenWRT - не единственная подходящая операционная система. Может случится так, что ваш роутер хорошо шьется, скажем, прошивкой Padavan, Pandora или аналогичной.

### Покупка нового роутера

Имевшийся у меня TP-Link Archer C20, хоть и подходил для моих экспериментов, но в виду особенностей чипа, при работе под OpenWRT, у него не работает WIFI в режиме 5GHz. Переходить обратно на 2.4GHz мне не хотелось, поэтому я полез искать ему замену.

Несмотря на то что рынок SOHO роутеров, как мне видится, испытывает определенную стагнацию, в диапазоне $70-110 можно найти что-то хорошее. Если перед вами тоже встанет вопрос выбора, рекомендую сравнивать асортимент магазина с [таблицей совместимости](https://openwrt.org/toh/views/toh_available_864) на сайте OpenWRT. В ней же указаны потенциальные проблемы, если они имеются.

В сети будете часто встречать советы о покупке различных моделей Keenetic. Стоят они начиная от $120, и часто позиционируются именно как роутеры для гиков. Прошивать их, скорее всего, не будет смысла, потому что родная операционка уже много чего умеет.

Я же остановился на Xiaomi Mi Router 3 Pro - одном из мощных и в то же время доступных устройств.

<LightBox>
    <img src={require('./assets/mir3p.png')} alt="Xiaomi Mi Router 3 Pro" />
</LightBox>

- **CPU**: 880 MHz
- **Flash**: 256 MB NAND
- **RAM**: 512 MB
- **USB**: 1 x 3.0
- **Gbit** ports: x4

### Прошивка роутера

Важно понимать, что неправильно прошив роутер, его можно закирпичить, и чтобы снова вернуть его в работу, придется из чего-то собирать или покупать программатор, разбирать роутер, подпаиваться к мат плате... Другими словами, все делаем максимально аккуратно и не ленимся потратить вечер на изучение инструкций.

В моем случае, хоть я и делал все с хирургической точностью, OpenWRT не встала с первого раза, и если бы я предварительно по совету не прошил кастомный загрузчик, то роутер отправился бы на полку до лучших времен. С кастомным загрузчиком есть возможность зажатием кнопки Reset загрузится в его меню и попытаться оживить умершее устройство. Поэтому, как я и сказал, лучше не спешить и точно понимать что нужно делать.

Для Mi R3P процесс прошивки выглядит примерно следующим образом:

1. Получаем SSH доступ, зарегистрировшить на сайте производителя и скачав бинарник.
2. Этот бинарник нужно закинуть на флешку отформатированную в FAT32 и вставить ее в роутер.
3. Туда же, в корень флешки, ложим установочные файлы OpenWRT.
4. С зажатой кнопкой Reset включить питание роутера.

Теперь можно ходить на роутер по SSH:

```bash
ssh root@192.168.31.1
```

Да, Xiaomi почему-то дают своим роутерам нестандартные IP адреса. 🤷‍♂️

В Wiki OpenWRT есть страницы на большинство поддерживаемых усторойств, поэтому просто следуем инструкции для [Mi R3P](https://openwrt.org/toh/xiaomi/xiaomi_r3p_pro):

```bash
cd /extdisks/sda1 #(can be different if you remove and reinsert the usb stick)
mv openwrt-ramips-mt7621-xiaomi_mir3p-squashfs-factory.bin factory.bin # give it a shorter name
nvram set flag_try_sys1_failed=1
nvram set flag_try_sys2_failed=0
nvram set flag_boot_success=0
nvram commit
dd if=factory.bin bs=1M count=4 | mtd write - kernel1
mtd erase rootfs0
mtd erase rootfs1
mtd erase overlay
dd if=factory.bin bs=1M skip=4 | mtd write - rootfs0
reboot
```

После перезагрузки прошивка будет установлена, роутер сменит IP на `192.168.1.1`. Можно открыть адрес в браузере или подключиться по SSH.

### Интерфейс LuCI

OpenWRT имеет веб интерфейс, который называется LuCI - это легковесный UI, написанный на Lua. У него есть интеграция со многими сетевыми инструментами.

LuCI поддерживает сторонние темы, поэтому я уже накатил `luci-theme-material`. На дефолтную тему мне, как человеку работающему с интерфейсами, тяжко смотреть. :)

<LightBox>
    <img src={require('./assets/openwrt-luci.png')} alt="LuCI OpenWRT" />
</LightBox>

Здесь есть много чего интересного, но ваша сборка может отличаться от моей, поэтому части пунктов меню может не быть и нужный софт придется доустанавливать вручную.

### Про установку и настройку пакетов

OpenWRT имеет собственный пакетный менеджер, который называется `opkg`. Работа с ним похожа на `apt`, и не должна вызвать трудностей.

```bash
opkg update # Обновляем список доступных пакетов
opkg install luci-theme-material # Устанавливаем нужный пакет
```

С настройкой же все немного интересней. Во всей системе используется унифицированный синтаксис конфигов, предоставляемый системой [UCI](https://openwrt.org/docs/guide-user/base-system/uci) - Unified Configuration Interface. UCI - это одновременно и система конфигов, и одноименная консольная команда, позволяющая как-то автоматизировать чтение/запись конфигов.

Через веб-интерфейс производить настроку не всегда удобно, особенно если документация описывает шаги именно как правки конфигов. Поэтому чаще придется подключаться к роутеру по SSH, что-то править и перезапускать сервисы.
